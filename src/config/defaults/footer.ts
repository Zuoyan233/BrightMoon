import type { FooterConfig } from "../../types/config";

export const defaultFooterConfig: FooterConfig = {
	enable: true, // 启用页脚
	customHtml:
		"This platform is a personal blog and is intended solely for learning and research purposes", // 自定义页脚 HTML 内容
};
